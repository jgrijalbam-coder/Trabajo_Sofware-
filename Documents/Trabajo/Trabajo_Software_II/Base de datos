CREATE DATABASE hotel_boutique;
USE hotel_boutique;

CREATE TABLE roles (
    id_rol INT AUTO_INCREMENT PRIMARY KEY,
    nombre_rol VARCHAR(50) UNIQUE NOT NULL
);

INSERT INTO roles (nombre_rol) VALUES
('administrador'),
('recepcionista'),
('cliente');

CREATE TABLE usuarios (
    id_usuario INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    apellido VARCHAR(100),
    email VARCHAR(100) UNIQUE NOT NULL,
    telefono VARCHAR(20),
    direccion VARCHAR(150),
    password_hash VARCHAR(255) NOT NULL,
    id_rol INT NOT NULL,
    estado BOOLEAN DEFAULT TRUE,
    intentos_login INT DEFAULT 0,
    bloqueado BOOLEAN DEFAULT FALSE,
    fecha_registro DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (id_rol) REFERENCES roles(id_rol)
);

CREATE TABLE tipos_habitacion (
    id_tipo INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL,
    descripcion TEXT,
    capacidad INT,
    precio_base DECIMAL(10,2)
);

INSERT INTO tipos_habitacion (nombre, descripcion, capacidad, precio_base)VALUES
('estandar','Habitación estándar',2,120000),
('suite','Suite de lujo',2,250000),
('familiar','Habitación familiar',4,300000);

SELECT * FROM tipos_habitacion;

CREATE TABLE habitaciones (
    id_habitacion INT AUTO_INCREMENT PRIMARY KEY,
    numero INT UNIQUE NOT NULL,
    id_tipo INT,
    piso INT,
    estado ENUM('disponible','reservada','ocupada','mantenimiento') 
    DEFAULT 'disponible',
    
    FOREIGN KEY (id_tipo) REFERENCES tipos_habitacion(id_tipo)
);

INSERT INTO habitaciones (numero, id_tipo, piso, estado) VALUES
(101,1,1,'disponible'),
(102,1,1,'disponible'),
(103,1,1,'disponible'),
(104,1,1,'reservada'),
(105,1,1,'disponible'),
(106,1,1,'disponible'),

(201,1,2,'disponible'),
(202,1,2,'mantenimiento'),
(203,1,2,'disponible'),
(204,1,2,'disponible'),
(205,2,2,'ocupada'),
(206,2,2,'disponible'),

(301,1,3,'disponible'),
(302,1,3,'disponible'),
(303,1,3,'disponible'),
(304,2,3,'disponible'),
(305,2,3,'disponible'),
(306,2,3,'disponible'),

(401,3,4,'disponible'),
(402,3,4,'disponible'),
(403,3,4,'disponible'),
(404,3,4,'ocupada'),
(405,3,4,'disponible'),
(406,3,4,'disponible');

SELECT * FROM habitaciones;

CREATE TABLE reservas (
    id_reserva INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    fecha_inicio DATE NOT NULL,
    fecha_fin DATE NOT NULL,
    estado ENUM('pendiente','confirmada','cancelada','finalizada'),
    total DECIMAL(10,2),
    fecha_creacion DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (id_usuario) REFERENCES usuarios(id_usuario)

);


CREATE TABLE detalle_reserva (
    id_detalle INT AUTO_INCREMENT PRIMARY KEY,
    id_reserva INT,
    id_habitacion INT,
    precio_noche DECIMAL(10,2),
    
    FOREIGN KEY (id_reserva) REFERENCES reservas(id_reserva),
    FOREIGN KEY (id_habitacion) REFERENCES habitaciones(id_habitacion)
);


CREATE TABLE facturas (
    id_factura INT AUTO_INCREMENT PRIMARY KEY,
    id_reserva INT,
    subtotal DECIMAL(10,2),
    impuestos DECIMAL(10,2),
    total DECIMAL(10,2),
    fecha_emision DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_reserva) REFERENCES reservas(id_reserva)
);

CREATE TABLE pagos (
    id_pago INT AUTO_INCREMENT PRIMARY KEY,
    id_factura INT,
    monto DECIMAL(10,2),
    metodo_pago ENUM('efectivo','tarjeta','transferencia'),
    estado ENUM('pendiente','pagado'),
    fecha_pago DATETIME,
    FOREIGN KEY (id_factura) REFERENCES facturas(id_factura)
);

CREATE TABLE notificaciones (
    id_notificacion INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    mensaje TEXT,
    tipo VARCHAR(50),
    estado ENUM('enviado','leido'),
    fecha DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_usuario) REFERENCES usuarios(id_usuario)
);
select *from reservas;
 select * from usuarios;
  select * from habitaciones;
 
 ALTER TABLE habitaciones
ADD COLUMN vista VARCHAR(50) NOT NULL DEFAULT 'ciudad';
